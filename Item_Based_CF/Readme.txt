The system takes the input file(ratings.txt) and the file should be placed in the root folder of the project. If you wish to change the file name or the path, need to give the full path in the main class.

The input should contain the rating in the following format,
UserId  MovieId  Rating

The output will be generated in the root folder with the following format.
UserId  MovieId  Rating